# Beta signing key

`signing/soundalert-v2-beta.keystore` is a beta-only test signing key.

It is deliberately used only with application ID `com.bridgeconn.soundalert.v2beta` so beta builds
can be installed and updated reliably from GitHub Actions. Do not reuse this key for the final
production package or Play Store release.
